# Deduplication pipeline

1. Convert all datasets to common encoding
2. Compute similarity matrices on the common-encoded datasets
3. Find duplicates and deduplicate
4. Create datasets with original encodings based on the deduplicated common-encoded datasets
